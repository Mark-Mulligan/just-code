import { stringifyExerciseSolution } from '../../utils/practiceProblemsHelp';

const evenOrOdd = (num: number) => {
  return num % 2 === 0;
};

export const evenOrOddSolution = `const evenOrOdd = (num) => {
  return num % 2 === 0;
};`;
