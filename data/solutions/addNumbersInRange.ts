export const addNumbersInRangeSolution = `const addNumbersInRange = (startingNum, endingNum) => {
  let total = 0;
  for (let i = startingNum; i <= endingNum; i++) {
    total += i;
  }

  return total;
};`;
